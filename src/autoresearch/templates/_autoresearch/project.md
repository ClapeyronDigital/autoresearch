# Project: <name>

## Description

Brief description of what the model does and what problem it solves.

## Contract: model ↔ eval

`Model.predict(x)` receives:
  - `x`: <describe input type and shape>

Returns:
  - <describe output type and shape>

`evaluate(model)` calls `model.predict()` on test data and returns a float
(higher is better).

## Baseline

- Baseline metric: <value>
- Description of the baseline model/approach.

## Insights

- What has been tried, what works / doesn't work.
